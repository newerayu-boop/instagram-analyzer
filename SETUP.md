# Система «ссылка → карусель» — пошаговая инструкция

Ты кидаешь ссылку на твит — получаешь готовую карусель (картинки + текст + прогрев)
под нужный из 4 аккаунтов. Ниже — как запустить это за один вечер.

---

## Что уже готово в репозитории

| Компонент | Файл | Что делает |
|-----------|------|-----------|
| Мастер-воркфлоу | `.claude/skills/tweet-to-carousel/SKILL.md` | Инструкция, по которой я работаю с каждой ссылкой |
| Шаблон аккаунта | `accounts/_TEMPLATE.md` | «Скилл» одного аккаунта (тема, голос, дизайн, прогрев) |
| Пример аккаунта | `accounts/example-mindset.md` | Как выглядит заполненный профиль |
| Движок дизайна | `carousel/render.mjs` | Рендерит слайды в готовые PNG 1080×1350 |
| Готовые слайды | `carousel/output/` | Сюда падают картинки, готовые к публикации |

---

## Фаза 1 — Запуск (быстро, без доступов)

### Шаг 1. Опиши 4 аккаунта (это и есть «скилл на аккаунт»)
Два пути — выбери, что быстрее:

**А) Я анализирую сама (минимум твоих усилий).**
Пришли мне 4 Instagram-username. Я проанализирую посты каждого аккаунта
(инструментом, который уже есть в репо) и сама заполню `accounts/<username>.md`:
тему, голос, стиль. Ты только пробежишься глазами и поправишь пару строк.

**Б) Заполнить шаблон руками (5 минут на аккаунт).**
Скопируй `accounts/_TEMPLATE.md` → `accounts/<username>.md` и заполни. Самое важное —
раздел 4 (цвета/шрифты) и раздел 7 (пример поста «как надо»).

> Итог фазы: 4 файла в `accounts/`. Это долгоживущая память системы — заполняется раз.

### Шаг 2. Проверка на одном твите
Кинь мне ссылку на любой твит. Я:
1. прочитаю его,
2. выберу подходящий аккаунт,
3. напишу карусель в его голосе,
4. отрендерю PNG-слайды,
5. дам текст поста + прогрев.

Посмотри результат, скажи что поправить (цвет, тон, длина). Я подкручу профиль —
дальше все посты будут в этом стиле автоматически.

### Шаг 3. Рабочий режим
Дальше просто кидаешь ссылку. Варианты:
- **«вот ссылка»** → я сам выберу аккаунт и сделаю пакет.
- **«ссылка, аккаунт 2»** → сделаю строго под указанный.
- **«ссылка, на все 4»** → сделаю 4 разные версии параллельно, каждая под свою нишу.

---

## Фаза 2 — Автопостинг (опционально, когда захочешь «без тебя»)

Чтобы карусели уходили в Instagram сами, нужен один из вариантов (нужны доступы):

1. **Планировщик (проще):** Buffer / Publer / Later. Даёшь API-ключ — я кладу готовые
   слайды и текст в очередь по расписанию на нужный аккаунт.
2. **Instagram Graph API (мощнее):** нужны бизнес-аккаунты, привязанные к Facebook-странице,
   и токен. Тогда постим напрямую, без сторонних сервисов.

Скажи, когда дойдём до этого — настроим отдельным шагом. До тех пор система выдаёт
готовый пакет, а ты публикуешь в один клик.

---

## Ручной запуск рендера (если нужно)

```bash
node carousel/render.mjs carousel/specs/<имя>.json
# слайды появятся в carousel/output/<account>/
```

Формат `spec.json` описан вверху `carousel/render.mjs`.

---

## Частые правки профиля
- Не тот тон → правим раздел 2 (голос) в `accounts/<username>.md`.
- Не тот вид → правим раздел 4 (цвета/шрифты).
- Мало/много слайдов → раздел 3 (формат карусели).
